#include <stdio.h>

/**
 * main - Entry point
 * @argc: args count
 * @argv: args array
 * Return: Always 0
 */
int main(int argc, char *argv[])
{
	int a;

	for (a = 0; a < argc; a++)
		printf("%s", argv[i]);
	return (0);
}
